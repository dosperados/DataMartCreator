<UserControl x:Class="DMCApp.Views.TableDefineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:DMCApp.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top: Filter -->
        <ui:TextBox Grid.Row="0" Margin="5" PlaceholderText="Filter by Dest Table..."
                    Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Middle: List -->
        <ListView Grid.Row="1" ItemsSource="{Binding TableDefinitions}" SelectedItem="{Binding SelectedTable}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Dest Table" DisplayMemberBinding="{Binding DestMainTableName}"/>
                    <GridViewColumn Header="Source Table" DisplayMemberBinding="{Binding SourceMainTableName}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Bottom: Add Button -->
        <ui:Button Grid.Row="2" Margin="5" Content="Add New" Icon="{ui:SymbolIcon Add24}"
                   Command="{Binding AddTableCommand}" HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
